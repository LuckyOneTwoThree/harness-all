# 运维工作台进度日志（Progress Log）

> 这是短期工作台记忆，记录当前的部署流水线执行到哪一步了，有没有没处理的报警。
> 每次会话开始时必读此文件，了解上一次你干到了哪里。

## 当前正在处理的高危/核心变更（Active Ops Tasks）

- `[例如：正在进行 v2.3 的蓝绿发布，目前流量灰度了 20%]`
- `[例如：正在排查某某慢查询导致的 DB CPU 报警]`

## 已被拦截或回滚的变更（Blocked/Rollbacked）

- `[例如：之前尝试应用的一个 Terraform 脚本因为缺乏 IAM 权限失败了，待处理]`
- `[例如：昨天的发版由于 OOM 被回滚，等待开发修复]`

## 今日待办（To-Do）

- `[ ]` 查阅 `docs/handoff/solo-to-ops.md` 开始新的交付部署。
- `[ ]` 更新一版 `ops-to-pm.md` 本周 SLA 报告给产品经理。

---

> 提示：保持精简，每次会话结束（session-end）请更新此文件。
