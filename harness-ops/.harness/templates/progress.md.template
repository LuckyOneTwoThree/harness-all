# Ops workbench progress log (Progress Log)

> This is short-term workbench memory, recording where the current deployment pipeline stands and whether there are unhandled alerts.
> Read this file at the start of every session to understand where you left off last time.

## Active high-risk / core changes (Active Ops Tasks)

- `[e.g., currently rolling out v2.3 via blue-green, traffic is at 20% canary]`
- `[e.g., investigating a DB CPU alert caused by a slow query]`

## Blocked or rolled-back changes (Blocked/Rollbacked)

- `[e.g., a Terraform script attempted earlier failed due to missing IAM permissions, pending follow-up]`
- `[e.g., yesterday's release was rolled back due to OOM, waiting for dev to fix]`

## Today's to-do (To-Do)

- `[ ]` Check `docs/handoff/solo-to-ops.md` to start a new delivery deployment.
- `[ ]` Update `ops-to-pm.md` with this week's SLA report for the PM.

---

> Tip: keep it concise; update this file at every session-end.
