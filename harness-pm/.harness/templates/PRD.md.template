# harness-pm — Product requirements document (PRD skeleton)

> Source: produced by the design-prd skill in module 3 (ideation and design).
> Load timing: read by engineering handoff / acceptance and release.
> Positioning: the core contract between PM and engineering, driving UI frontend generation and backend API design.
> This skeleton is created during the setup phase; the full content is filled in by the design-prd skill.
>
> **Companion file**: `docs/product/prd.json` — machine-consumable structured data (features/pages/entities/user_flows/NFR/tracking_plan/traceability), generated by design-prd alongside this PRD.md. Downstream Skills (design-brief, brainstorming) consume both files.

## Document info

| Item | Value |
|------|-----|
| PRD level | L / S / X (auto-leveled by design-prd) |
| Version | v0.1 (skeleton) |
| Status | Pending fill by design-prd skill |
| Created | YYYY-MM-DD |
| PRD.md path | docs/product/PRD.md |
| prd.json path | docs/product/prd.json |

## 1. Background and goal

[Pending fill by design-prd skill]

## 2. Functional requirements

| Number | Feature | Priority | Acceptance criteria |
|------|------|--------|---------|
| F-001 | [feature name] | P0 | AC-001: [testable description] |

## 3. Simplified interaction and data (PRD-L) / Full design (PRD-S/X)

> PRD-L: simplified single-line page flow + entity core fields + API name/method/path only.
> PRD-S/X: full page flow diagram, 5-state table, complete data model, full API detail template.
> See design-prd Reference/prd-structure.md for tier-specific rules.

### 3.1 Page flow (simplified)

[Page A] → action → [Page B]

### 3.2 Data model (simplified)

| Entity | Core fields | Description |
|--------|------|------|
| [entity name] | id, name, status, [business field] | [one-line description] |

### 3.3 Interface list (simplified)

| API name | Method | Path |
|------|------|------|
| [api name] | GET/POST | /api/v1/[resource] |

## 4. Constraints and requirements

> Merged section (PRD-L): technical constraints + known limitations + NFR (performance/security only).
> PRD-S/X: split into Section 4 (Boundaries & Constraints) + Section 5 (NFR, 4 dimensions).

[Performance / security / accessibility requirements]

## 5. Out of scope

[Explicit boundaries]

## 6. Acceptance criteria

> Acceptance criteria must be **testable** — not "easy to use", but "input X returns Y".
> PRD-L: Happy Path only. PRD-S/X: Happy Path + boundary + exception + compatibility.

- AC-001: [to be filled]
- AC-002: [to be filled]

## 7. Release and appendix

> Merged section (PRD-L): release strategy + glossary + change log + open issues.
> PRD-S/X: split into Section 8 (Release & Operations) + Section 9 (Appendix).

| Date | Revision | Reason |
|------|---------|------|
| YYYY-MM-DD | Create skeleton | setup workflow |

---

> **Note**: this file is a skeleton; the full PRD is generated by the design-prd skill.
> design-prd auto-fills each section based on upstream outputs (user research / market analysis / business strategy).
> The PRD must pass 4 quality gates: completeness / consistency / ambiguity elimination / traceability.
> PRD-L uses relaxed validation rules (see design-prd SKILL.md "PRD-L validation" section).
