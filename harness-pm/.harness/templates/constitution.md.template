# constitution.md — 项目宪法（模板）

> **角色说明**：本文件是**你自己的项目**的宪法，约束本项目的产品工作。
> 每个项目独有的不可协商原则。
> 与 AGENTS.md 的分工：AGENTS.md 是通用工作规则（所有项目相同），constitution.md 是项目特定约束。
> 加载时机：AGENTS.md 之后，首次交互时读。

## 如何使用本文件

1. 项目立项时，根据产品特性填写下方条款
2. 每条款必须是**可验证的**（能通过检查确认）
3. Agent 在 PLAN 阶段必须检查任务是否符合宪法
4. 宪法冲突时，宪法优先于用户当前指令（除非用户明确说"修改宪法"）

## 项目特定原则

<!-- 根据项目实际情况填写，以下是示例 -->

### 原则 1：PRD 必须通过 4 道质量门禁
所有 PRD 产出必须通过完整性/一致性/歧义消除/可追溯性 4 道门禁，不通过不交接工程。

**验证方式**：verify skill 检查 PRD 门禁通过记录

### 原则 2：关键决策必须人类审批
以下决策必须人类确认，AI 不可代决：
- 方案选择（A 方案 vs B 方案）
- 优先级排序（功能优先级/需求优先级）
- 策略方向（产品定位/增长策略）

**验证方式**：产出文件中有 `human_approved: true` 标记

### 原则 3：上线前必须有埋点方案
所有功能上线前必须有对应的埋点方案和指标体系，否则不予发布。

**验证方式**：launch 工作流前置检查埋点方案存在

### 原则 4：[按需添加]

## 宪法检查点（PLAN 阶段必查）

- [ ] 当前任务是否违反"PRD 必须过门禁"？
- [ ] 关键决策是否已人类审批？
- [ ] 是否涉及上线？埋点方案准备好了吗？
- [ ] [其他项目特定检查]

## 修订记录

| 日期 | 修订内容 | 原因 |
|------|---------|------|
| YYYY-MM-DD | 初始版本 | 项目立项 |
