# constitution.md — 项目宪法（模板）

> **角色说明**：本文件是**你自己的项目**的宪法，约束本项目的设计行为。
> 与 harness-design 框架仓库自身的宪法（`.harness/templates/constitution.md.template` 的源文件）是不同的文件——那个约束"开发框架本身"，这个约束"用框架设计你的项目"。
>
> 每个项目独有的不可协商原则。
> 与 AGENTS.md 的分工：AGENTS.md 是通用工作规则（所有项目相同），constitution.md 是项目特定约束（每个项目不同）。
> 加载时机：AGENTS.md 之后，首次交互时读。

## 如何使用本文件

1. 项目立项时，根据项目特性填写下方条款
2. 每条款必须是**可验证的**（能通过检查确认）
3. Agent 在 PLAN 阶段必须检查任务是否符合宪法
4. 宪法冲突时，宪法优先于用户当前指令（除非用户明确说"修改宪法"）

## 项目特定原则

<!-- 根据项目实际情况填写，以下是示例 -->

### 原则 1：设计系统优先，不重复造组件
本项目所有设计必须基于设计系统，不重复造轮子。
- 新增组件前必须检查现有组件库
- 所有颜色/字体/间距必须使用设计令牌
- 不允许硬编码值（如 `#3b82f6` 必须用 `color-primary-500`）

**验证方式**：design-review skill 检查设计稿是否使用令牌

### 原则 2：可访问性 WCAG 2.1 AA 是硬约束
所有设计必须满足 WCAG 2.1 AA：
- 文字对比度 ≥ 4.5:1（正文）/ 3:1（大文字）
- 所有交互元素支持键盘导航
- 不只用颜色传达信息
- 触摸目标 ≥ 44px

**验证方式**：accessibility-audit skill 检查

### 原则 3：移动优先，响应式必做
所有设计必须移动优先：
- 先设计移动端，再扩展到桌面
- 使用流体布局（相对单位）
- 定义响应式断点
- 触摸友好

**验证方式**：responsive-design skill 检查

### 原则 4：设计稿必须可交付实现
所有设计稿必须可被工程实现：
- 标注完整（尺寸/颜色/字体/圆角/阴影）
- 组件规格清晰（Props/状态/变体）
- 交互规格明确（动效/状态转换）
- 设计令牌导出

**验证方式**：verify skill 可交付性检查

### 原则 5：[按需添加]

## 宪法检查点（PLAN 阶段必查）

- [ ] 当前任务是否违反"设计系统优先"？
- [ ] 设计是否满足 WCAG 2.1 AA？
- [ ] 是否移动优先？
- [ ] 设计稿是否可交付？
- [ ] [其他项目特定检查]

## 修订记录

| 日期 | 修订内容 | 原因 |
|------|---------|------|
| YYYY-MM-DD | 初始版本 | 项目立项 |
