# constitution.md — 项目宪法（模板）

> **角色说明**：本文件是**你自己的项目**的宪法，约束本项目的增长运营行为。
> 与 harness-growth 框架仓库自身的宪法（`.harness/templates/constitution.md.template` 的源文件）是不同的文件——那个约束"开发框架本身"，这个约束"用框架运营你的项目"。
>
> 每个项目独有的不可协商原则。
> 与 AGENTS.md 的分工：AGENTS.md 是通用工作规则（所有项目相同），constitution.md 是项目特定约束（每个项目不同）。
> 加载时机：AGENTS.md 之后，首次交互时读。

## 如何使用本文件

1. 项目立项时，根据项目特性填写下方条款
2. 每条款必须是**可验证的**（能通过检查确认）
3. Agent 在 PLAN 阶段必须检查任务是否符合宪法
4. 宪法冲突时，宪法优先于用户当前指令（除非用户明确说"修改宪法"）

## 项目特定原则

<!-- 根据项目实际情况填写，以下是示例 -->

### 原则 1：增长是实验，每个动作有假设有度量
每个增长动作在执行前必须明确：
- 假设：做 X 会让 Y 提升 Z%
- 主要度量：实验成败的判定指标
- 护栏度量：防止副作用的监控指标

**验证方式**：spec.md 缺少假设或度量字段的实验不准进入 EXPERIMENT 阶段

### 原则 2：内容质量 > 数量，不做内容农场
- 每篇内容必须有独特观点或信息增量
- 不为 SEO 堆砌关键词、不生产同质化内容
- 内容必须对用户有真实价值

**验证方式**：内容生产 skill 质量门检查

### 原则 3：SEO 长期主义，不做黑帽
- 不做关键词堆砌、隐藏文本、链接农场
- 不刷点击、刷排名
- 遵循搜索引擎官方指南

**验证方式**：security.md 黑帽 SEO 禁令检查

### 原则 4：数据闭环，每个实验有结论
- 实验结束必须写结论（有效/无效/不确定）
- 结论写入 knowledge-base.md
- 有效 → 放大；无效 → 停止；不确定 → 重新设计

**验证方式**：measure skill 检查结论字段

### 原则 5：[按需添加]

## 宪法检查点（PLAN 阶段必查）

- [ ] 当前实验是否标注了假设和度量指标？
- [ ] 当前内容是否符合质量门（独特观点、用户价值）？
- [ ] 当前 SEO 操作是否合规（无黑帽手段）？
- [ ] 当前实验数据是否脱敏（无 PII）？
- [ ] [其他项目特定检查]

## 修订记录

| 日期 | 修订内容 | 原因 |
|------|---------|------|
| YYYY-MM-DD | 初始版本 | 项目立项 |
